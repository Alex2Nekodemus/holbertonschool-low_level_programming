#include "main.h"

/*
 * add - function adds to integers and return the result
 * @a - first integer
 * @b -second integer
 * return - c the result of this add
 */
int add(int a, int b)
{
	int c;

	c = (a + b);
	return (c);
}
